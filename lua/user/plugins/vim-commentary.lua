-- File: ~/.config/nvim/lua/plugins/telescope.lua

return {
  "tpope/vim-commentary",
  -- Optional: You can add a description and keys
  config = function()
    -- This plugin works out of the box, no setup is needed.
    -- The keymaps are enabled by default.
  end,
}
